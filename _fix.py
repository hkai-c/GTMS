import pathlib; p=pathlib.Path('e:/data_control/server/services/query_service.py'); c=p.read_text('utf-8'); c=c.replace('        QueryResponse(','        QueryResponse('); c=c.replace('        Response(','        Response('); c=c.replace('        ranking\n\n    def get_machine','        ranking\n\n    def get_machine'); c=c.replace('        ranking\n\n    def export','        ranking\n\n    def export'); c=c.replace('        items\n\n    def _apply','        items\n\n    def _apply'); c=c.replace('        query\n\n    def _apply_sorting','        query\n\n    def _apply_sorting'); c=c.replace('        query\n\n    def _task','        query\n\n    def _task'); c=c.replace('        {\n            "id": task.id','        {\n            "id": task.id'); p.write_text(c,'utf-8'); print('Fixed')
